{
  "name": "mailchimp-google-assistant",
  "description": "Manage MailChimp campaigns",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "author": "Caleb Kirksey",
  "engines": {
    "node": "~4.2"
  },
  "scripts": {
    "lint": "semistandard --fix \"**/*.js\"",
    "start": "firebase serve --only functions",
    "deploy": "firebase deploy --only functions",
    "validate": "npm ls"
  },
  "dependencies": {
    "actions-on-google": "^1.0.0",
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "mailchimp-api-v3": "^1.7.0",
    "precommit-hook": "^3.0.0"
  },
  "devDependencies": {
    "semistandard": "^9.1.0"
  },
  "pre-commit": [
    "lint",
    "validate",
    "test"
  ]
}
